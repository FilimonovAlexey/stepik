#include <stdio.h>

int main() {
  int n = 0;
  scanf ("%d", &n);
  int matrix[n][n];
  for (int i=0; i<n; i++) {
      for (int j=0; j<n; j++){
         if (i>j) printf ("%d ", i-j+1); 
         else printf ("%d ", j-i+1); 
      }
          
      printf ("\n");
  }

  return 0;
}