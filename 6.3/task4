#include <stdio.h>
#include <math.h>

int main() {
  double x1=0, y1=0, x2=0, y2=0;
  scanf("%lf%lf", &x1, &y1);
  scanf("%lf%lf", &x2, &y2);
  double r1 = sqrt (x1*x1+y1*y1);
  double r2 = sqrt (x2*x2+y2*y2);
  printf(r1<r2 ? "1" : "2");
  return 0;
}