#include <stdio.h>

int main() {
  double sum = 0;
  int n = 0;
  scanf ("%d", &n);
  for (int i=0; i<n; i++) {
      int temp = 0;
      scanf ("%d", &temp);
      sum += temp;
  }
  printf ("%.2f", sum/n);
  return 0;
}