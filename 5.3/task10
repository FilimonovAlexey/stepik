#include <stdio.h>
#include <math.h>

int main() {
  int k=0, n=0, x=0;
  scanf("%d", &k);
  for (int i=0; i<4; i++) {
  int x=0;
  scanf("%d", &x);
  int res = (x+k)%26;
  printf("%d ", res);
  }
  return 0;
}