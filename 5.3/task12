#include <stdio.h>
#include <math.h>

int main() {
  int k=0;
  scanf("%d", &k);
  int res = (k/1000+3)%10*10+(k%1000/100+3)%10+(k%100/10+3)%10*1000+(k%10+3)%10*100;
  printf("%d", res);
  return 0;
}