#include <stdio.h>
#include <string.h>

int main() {
    char str1[40]; 
    char str2[20];    
    scanf("%s%s", str1, str2);
    if (strcmp (str1, str2)) printf("no");
    else printf("yes");
    return 0;
}