#include <stdio.h>

int main() {
    char str[100];
    int i=0;
    while (1) {
        int count = scanf ("%s", &str);
        if (count <=0) break;
        int length = strlen(str);        
        if (str[0]==str[length-1]) printf ("%s ", str);
    }
    return 0;
}